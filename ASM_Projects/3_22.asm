DATA SEGMENT
A1 DW 0FFFBH
A2 DW 0FFFFH
B1 DW ?
B2 DW ?
DATA ENDS
CODE SEGMENT
MAIN PROC FAR
ASSUME CS:CODE,DS:DATA
START:
	MOV AX,DATA
	MOV DS,AX
	MOV AX,A1
	MOV DX,A2
	CMP DX,0
	JNS JUDGE
	NEG DX
	NEG AX
	SBB DX,0
JUDGE:
	MOV B1,AX
	MOV B2,DX
	RET
	;INT 20H
MAIN ENDP
CODE ENDS
END START